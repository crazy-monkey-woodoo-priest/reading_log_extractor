require 'spec_helper'

describe ReadingLogExtractor do
  it 'has a version number' do
    expect(ReadingLogExtractor::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
